# Domain Docs

This repository uses a single-context layout.

Before exploring a domain, read `CONTEXT.md` at the repository root when present and read relevant decisions under `docs/adr/`. If those files do not exist, proceed without creating them solely for setup.

Use the glossary vocabulary from `CONTEXT.md` in issues, specs, and tests. Surface conflicts with existing ADRs rather than silently overriding them.
